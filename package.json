{
  "name": "mailgofer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create mail_api",
    "db:migrate": "wrangler d1 execute mail_api --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
